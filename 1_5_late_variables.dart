/*void main() {
  late final String name;
  // final, var 앞에 붙이는 수식어
  // 초기값 없이 변수 선언 가능하게 함.
  // data fetching에 유용

  // do something

  print(name);
  // 값을 넣기 전에는 사용하면 안됨을 알려줌. (null safety 같은 기능)

  name = 'moca';
  print(name);
}
*/