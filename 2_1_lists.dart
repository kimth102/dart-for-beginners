/*void main() {
  var giveMeFive = true;
  var numbers = [
    1,
    2,
    3,
    4,
    if (giveMeFive) 5,
  ];
  //List<int> numbers2 = [1, 2, 3, 4];
  // , 로 끝내면 자동으로 정렬됨.

  numbers.add(5);
  print(numbers.first);

  var numbers2 = [
    1,
    2,
    3,
    4,
    if (giveMeFive) 5,
  ];
  // collection if
  // 조건에 따라 리스트 요소를 추가
  // if (giveMeFive) { numbers2.add(5); } 와 동일

  print(numbers2);
}
*/