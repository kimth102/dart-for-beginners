/*void main() {
  var name; // 변수 선언 시 초기값을 지정하지 않음.
  dynamic name2; // dynamic 타입으로 변수 선언

  // dynamic : 여러가지 타입을 가질 수 있는 타입
  // 사용이 추천되지는 않음.
  // 1. 타입을 추측할 수 없는 경우
  // 2. dynamic이 유용할 경우

  name = 'moca';
  name = 10;
  name = true;

  if (name is String) {
    // 타입을 확인한 후에는 해당 타입의 메소드 사용 가능
  } else if (name is int) {}
}
*/